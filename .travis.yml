language: cpp

compiler:
        - clang
        - gcc

before_install:
        - sudo apt-get update -qq
        - sudo apt-get install -y libssl-dev libprotobuf-dev libboost-dev
 
script: ./scripts/travis_build.sh
